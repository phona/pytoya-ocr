name: build-and-deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Download ONNX models from Release
        uses: robinraju/release-downloader@v1.8
        with:
          repository: phona/pytoya-ocr
          tag: v1.0
          fileName: "*.onnx"
          out-file-path: service/models/

      - name: Arrange model files
        run: |
          mkdir -p service/models/det/onnx service/models/rec/onnx
          mv service/models/det.onnx service/models/det/onnx/model.onnx
          mv service/models/rec.onnx service/models/rec/onnx/model.onnx

      - name: Log in to ghcr.io
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build and push
        run: |
          cd service
          docker build -t ghcr.io/${{ github.repository }}/ocr-service:latest .
          docker push ghcr.io/${{ github.repository }}/ocr-service:latest

      - name: Deploy to server
        uses: appleboy/ssh-action@v1
        timeout-minutes: 30
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          command_timeout: 30m
          script: |
            set -e
            cd /root/ocr-service
            docker compose pull
            docker compose up -d
            sleep 5
            curl -sf http://localhost:8090/health || exit 1
